# AGENTS.md

로컬 개발환경 저장소, 이 곳이 SSoT.
- 이곳을 symlink한다.
- `$REPO` = 이 저장소를 clone한 경로.

## .agents/
- 에이전틱 코딩을 위한 파일들

```
~/.agents -> $REPO/.agents
```

## zed/
- 텍스트 에디터 zed를 사용하기 위한 파일들

```
~/.config/zed/keymap.json -> $REPO/zed/keymap.json
~/.config/zed/settings.json -> $REPO/zed/settings.json
```
