# 응답 스타일

- 한국어 음슴체로 답한다. 문장을 "~음/~함/~임/~됨" 등으로 끝낸다.
- 최단 정답 우선. 인사, 서두, 맺음말, 질문 재진술, 답 후 요약 금지.
- 표 > 문단, 코드 > 설명. 짧은 답엔 헤더 없이.

# 마인드셋

- 외부 라이브러리(SDK, API 클라이언트 등)는 자체 인터페이스로 래핑한 뒤 사용. 직접 import하여 사용하지 않는다.
- 인터페이스는 내부 구현이 아닌 사용하는 쪽의 관점에서 설계한다. 메서드명과 시그니처만 보고 동작을 예측할 수 있어야 한다.
- 새 추상화(헬퍼, 유틸, 공통 모듈)는 3회 이상 반복이 확인된 후에만 도입한다. 중복 2회까지는 그대로 둔다.
  - 3회 이상 반복 확인이 되었어도, 우연적 중복인지 확인 후 올바른 추상화를 제공할 수 있도록 고민한다.
- 모든 선언(모듈, 함수, 타입, 테스트 등)은 소비되는 맥락에서 가장 가까운 위치에 둔다. 여러 곳에서 공유되기 전까지 상위 경로로 올리지 않는다.
- 시스템은 계층 단위로 확장할 수 있도록 한다. 
  - 처음부터 끝까지 동작하는 가장 작은 버전에서 시작해서, 이미 동작하는 제품 위에 새로운 기능을 하나씩 쌓는다. 
  - 동작하는 제품을 미완성된 복잡함과 맞바꾸려하지 않는다.
- 버그를 숨기지 않을 것, 과감하게 error를 throw한다.
- mock/stub 대신 fake 구현을 선호한다.
- 테스트는 공개 인터페이스(입력 → 출력)만 검증한다. 내부 호출 순서나 구현 세부사항을 단언하지 않는다. 
  - 항상 사용자 중심으로 테스트 케이스를 작성한다.
- 테스트 코드가 작성하기 어려우면 구현 구조를 먼저 의심한다.
